# Design reference — the original .dc.html mockups and poster artwork the site
# was rebuilt from. Kept on disk for reference, out of the repo: it is 8.7MB
# that would be cloned and redeployed on every change without ever being served.
source/
*.zip

# Local preview config
.claude/

# OS and editor noise
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*.swp
*~

# Logs
*.log
